# Reporter example in JS

This reporter implementation uses data from mempool.space.

## How to
Install:
```sh
yarn install
```

Create a `.env` file with the a gas payer and corresponding public/private key pair. (see `envsample`)


Run:
```
node index.js
```
